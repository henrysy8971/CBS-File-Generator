{
	"interfaces": {
		"ORDER_INTERFACE": {
			"name": "ORDER_INTERFACE",
			"dataSourceQuery": "SELECT o FROM Order o LEFT JOIN FETCH o.lineItems WHERE o.status = 'ACTIVE' AND (:lastId IS NULL OR o.id > :lastId) ORDER BY o.orderId",
			"keysetColumn": "orderId",
			"xsdSchemaFile": "order_schema.xsd",
			"chunkSize": 1000,
			"outputFormat": "XML",
			"outputFileExtension": "xml",
			"enabled": false,
			"description": "Order interface - uses BeanIO mapping and optional XSD validation"
		}
	}
}
